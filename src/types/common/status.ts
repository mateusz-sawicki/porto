export enum PatientStatus {
  Active = 'Active',
  Inactive = 'Inactive'
}

export enum TreatmentPlanStatus {
  Draft = 'Draft',
  InProgress = 'InProgress',
  Completed = 'Completed',
  Cancelled = 'Cancelled'
}

export enum DialogMode {
  Add = 'add',
  Edit = 'edit'
}